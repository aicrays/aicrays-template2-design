
import React from "react";

const HeroVisual = () => {
  // This component is now empty since we removed all icon-based content
  // The visual elements are now handled by HeroBackground with floating bubbles
  return null;
};

export default HeroVisual;
